============
Installation
============

At the command line::

    $ easy_install ddexreader

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv ddexreader
    $ pip install ddexreader
